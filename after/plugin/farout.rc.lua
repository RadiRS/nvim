vim.opt.termguicolors = true
vim.cmd("colorscheme farout")

-- local status, n = pcall(require, "frout")
-- if (not status) then 
--  print('theme not found')
--  return 
-- end

-- n.setup({
--  commit_italics = true
-- })
